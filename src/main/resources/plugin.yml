name: RandomQuotePlugin
version: '${project.version}'
main: voidpointer.example.localemodule.RandomQuotePlugin
api-version: 1.18
prefix: DEBUG

commands:
  random-quote:
    aliases: ['randomquote', 'rq']
    description: 'Gets a random quote from quotable.io'
